kind: PersistentVolume
apiVersion: v1
metadata:
  name: pv-volume-web
  labels:
    type: local
    app: test-app
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/tmp/data"